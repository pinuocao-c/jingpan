净盘 0.3.2 是一次文件整理体验与本机存储方式更新。重点是让大量图片、视频、聊天附件和重复文件更容易看清、选择和处理，同时把本机净盘的安装副本、设置与缓存集中到项目目录。

## 本次更新

- 个人文件与微信、QQ 聊天文件支持“列表 / 方块”视图切换；方块视图会放大图片缩略图和视频首帧。
- 图片与视频支持应用内连续预览，可用左右方向键切换，并查看大小、修改时间和完整路径。
- 个人文件、聊天文件和重复文件都只保留上方一个“移入回收站”操作区，不再在页面底部重复显示。
- 新增本页全选、取消本页、清空选择，以及筛选范围外已选项目提醒；单次仍限制最多处理 2,000 个文件。
- 排序支持名称、大小、修改日期的升序与降序；列表/方块和排序偏好会自动记忆，新扫描会清除旧筛选条件。
- 分页支持直接输入页码并按 Enter 跳转，长列表不再只能逐页点击。
- 扫描结果显示距今时间；超过 30 分钟会提示重新扫描，减少文件已变化造成的失败。
- 文件移入回收站后会显示成功数量与容量、失败文件原因，并可直接打开回收站或前往安全清理。
- 聊天文件会分别统计 C 盘和其他磁盘的已选容量，避免把 D 盘删除量误认为 C 盘释放量。
- 微信文件识别扩展到 Windows 已登记在其他固定磁盘的文档目录，与 QQ 一样明确标注所在盘符。
- 设置页新增缓存与设置位置，可确认当前是否启用项目内存储并直接打开目录。
- 开发构建使用项目内 `.cache/` 保存 npm、Electron、electron-builder 和临时文件，减少在系统盘产生项目缓存。

## 安全边界

- 文件删除仍然只进入 Windows 回收站，不做永久删除；清空回收站后才会真正释放磁盘空间。
- 预览协议只允许读取本次扫描结果中身份复核通过的图片和视频，不能提交任意本地路径。
- 安装包仍禁止直接预览运行，只能打开所在位置后由用户自行确认。
- 微信、QQ 数据库、索引和配置仍不参与清理；加密图片不会尝试解密。
- C 盘到 D 盘迁移仍采用“复制、SHA-256 校验、再移除源文件”；任一步失败都会删除 D 盘未完成副本并保留 C 盘原件。

## 安装说明

旧版净盘会在设置页显示 `v0.3.2` 更新提醒。点击“下载更新”后会打开 GitHub 官方安装包下载页面，下载完成后运行安装程序即可。

当前安装包尚未进行商业代码签名，因此 Windows SmartScreen 可能显示“未知发布者”。请只使用本项目 GitHub Release 页面提供的安装包，并核对 SHA-256 摘要。
