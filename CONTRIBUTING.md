# 参与贡献

感谢你帮助改进净盘。涉及文件删除、应用卸载或系统目录的改动，需要比普通界面功能更严格地审查。

## 开发流程

1. 从 `main` 创建功能分支。
2. 安装依赖并运行 `npm run dev`。
3. 为路径校验、白名单或格式化逻辑补充测试。
4. 提交前依次运行：

```powershell
npm run typecheck
npm test
npm run build
```

## 安全相关要求

- 不允许从渲染进程接收任意删除路径。
- 新增清理项必须有明确、固定、可审查的目录白名单。
- 不跟随符号链接，不越过目标根目录。
- 清理目录和个人文件必须使用真实路径、扫描凭证或文件指纹复验。
- 不直接删除 `Windows`、`Program Files`、`ProgramData` 或用户文档。
- 新增不可恢复操作时，必须默认关闭并提供二次确认。
- 不使用字符串拼接启动 Shell 命令；外部程序必须使用参数数组或经过验证的登记入口。
