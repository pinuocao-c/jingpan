净盘 0.2.0 是一次面向“更容易看懂、更安全释放 C 盘空间”的功能更新，新增精确重复文件治理、Windows 真实个人目录识别、系统保留空间解释和更多官方空间管理入口。

## 本次更新

- 新增“重复文件”页面，只把大小相同且完整 SHA-256 一致的文件判定为重复
- 支持智能选择重复副本：每组默认保留最近修改的一份，所选文件移入 Windows 回收站
- 删除前再次校验文件身份、修改时间和内容哈希，并强制每组至少保留一份
- 重复文件扫描只处理 C 盘个人目录中 1 MB 以上的常见个人文件，跳过 OneDrive 等云目录以避免触发下载
- 个人文件新增 MP3、WAV、M4A、FLAC、AAC、OGG、WMA、Opus 等音频分类，以及独立的音乐目录筛选
- 个人文件与聊天目录改为读取 Windows 登记的真实“桌面、下载、文档、图片、视频、音乐”位置，系统文件夹迁移后也能正确识别
- 空间分析新增休眠文件、分页文件、应用交换文件、系统内存转储和以前的 Windows 安装占用说明
- 对系统级占用保持只读，并可直接打开 Windows 官方“清理建议”页面处理内存转储和旧版 Windows
- 设置页新增“Windows 清理建议”“自动存储感知”和“新内容保存位置”，既释放当前空间，也减少 C 盘再次变满
- 优化重复文件、系统空间和个人文件页面的提示、选择上限、可恢复说明与键盘操作

## 安全设计

- 重复文件不根据名称或扩展名猜测，必须完整读取内容并通过 SHA-256 校验
- 主进程拒绝删除一整组重复文件，准备保留的副本在操作前也必须重新通过身份和内容校验
- 文件只会移入 Windows 回收站，不做永久删除；单次最多处理 2,000 个文件
- OneDrive 等云目录仍可在个人文件页按元数据整理，但重复文件页不会读取其内容，避免在线文件被自动下载
- 休眠、虚拟内存、快速启动、系统回退和其他系统配置不会被净盘直接修改
- Windows 系统级项目交给微软官方设置或磁盘清理工具，由用户查看影响后确认
- 所有扫描、哈希与分类均在本机完成，不上传文件内容、路径或扫描结果

## 安装说明

旧版净盘会显示 `v0.2.0` 更新提醒。点击“下载更新”后，净盘会使用默认浏览器打开 GitHub 官方安装包下载，下载完成后请手动运行安装程序。

当前安装包尚未进行商业代码签名，因此 Windows SmartScreen 可能显示“未知发布者”。请只从本项目的 GitHub Release 页面下载安装包，并核对 GitHub 展示的 SHA-256 摘要。
