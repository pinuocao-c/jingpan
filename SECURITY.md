# 安全策略

## 支持范围

当前处于早期开发阶段，仅最新版本接受安全修复。

## 报告安全问题

请不要在公开 Issue 中披露可导致任意文件删除、路径越界、命令执行或权限提升的问题。请通过 GitHub 仓库的 Private vulnerability reporting 功能提交报告，并说明：

- 受影响版本
- 复现步骤
- 可能影响
- 建议修复方式（如有）

## 特别关注

净盘属于会修改本地文件的高风险工具。以下问题应按高优先级处理：

- 绕过清理类别白名单
- 跟随目录链接或连接点越界
- 扫描后通过替换目录连接点或同名文件绕过复验
- 向主进程注入任意路径或命令
- 绕过个人文件扫描结果执行删除
- 静默永久删除用户文件
- 远程内容获得 Electron 或文件系统权限
- 通过 `ELECTRON_RUN_AS_NODE`、`NODE_OPTIONS` 或调试参数改变打包程序行为
