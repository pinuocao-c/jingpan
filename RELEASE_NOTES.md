净盘 0.1.4 修复 QQ/NTQQ 自定义文件位置无法识别的问题，并让扫描结果明确区分“确实没有可管理文件”和“尚未识别到文件位置”。

## 本次更新

- 自动查找固定磁盘浅层目录中的 `Tencent Files`
- 支持 QQ/NTQQ 数据改到 D 盘等自定义位置
- 新增“选择 QQ 目录”，可手动指定 `Tencent Files`、其上级目录或账号目录
- 手动选择的有效 QQ 目录会保存在本机，后续扫描自动使用
- QQ 平台卡片会显示已识别位置、文件数量和其他磁盘占用
- 文件来源会标注所在盘符，避免误以为删除 D 盘文件能够释放 C 盘
- 未找到 QQ 位置时明确提示“这不等于 QQ 没有文件”

## 安全设计

- 只扫描自动识别或用户明确选择的 QQ 媒体、附件和接收文件目录
- 明确排除微信与 QQ 的消息数据库、索引、日志、锁文件和数据库旁路文件
- 不读取消息正文、联系人、聊天对象或文件内容
- 自动路径发现只查看固定磁盘的浅层目录名，不遍历文件内容
- 打开和删除前重新校验真实路径、文件身份、大小与修改时间
- 用户勾选的文件只会移入 Windows 回收站，不做永久删除

## 安装说明

旧版净盘会显示 `v0.1.4` 更新提醒。点击“下载更新”后，净盘会使用默认浏览器打开 GitHub 官方安装包下载，下载完成后请手动运行安装程序。

当前安装包尚未进行商业代码签名，因此 Windows SmartScreen 可能显示“未知发布者”。请只从本项目的 GitHub Release 页面下载安装包，并核对 GitHub 展示的 SHA-256 摘要。
