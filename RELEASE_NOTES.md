净盘 0.3.1 是一次文件管理效率更新，重点解决个人文件和聊天文件数量多时的预览、选择和翻页问题。

## 本次更新

- 个人文件、微信与 QQ 聊天文件新增“列表 / 方块”视图切换。
- 方块视图会放大图片和视频缩略图，更适合快速分辨照片、截图和视频封面。
- “移入回收站”操作新增到文件浏览区域上方，用户不需要滚动到底部也能处理已选文件。
- 底部仍保留同一操作区，长列表滚动后仍可直接执行。
- 分页控件支持键盘输入页码，输入后按 Enter 或移出输入框即可跳转。
- 迁移到 D 盘页面也支持页码输入，适合候选文件较多时快速定位。
- 所有新增操作继续使用扫描结果中的文件 ID，不向前端暴露任意删除路径。

## 安全边界

- 文件删除仍然只进入 Windows 回收站，不做永久删除。
- 安装包仍禁止直接预览运行，只能打开所在位置后由用户自行确认。
- 微信、QQ 数据库、索引、配置和加密内容仍不参与清理。
- C 盘到 D 盘迁移逻辑未改变，仍是复制、校验、再移除源文件，失败时保留 C 盘原件。

## 安装说明

旧版净盘会在设置页显示 `v0.3.1` 更新提醒。点击“下载更新”后会打开 GitHub 官方安装包下载页面，下载完成后运行安装程序即可。

当前安装包尚未进行商业代码签名，因此 Windows SmartScreen 可能显示“未知发布者”。请只使用本项目 GitHub Release 页面提供的安装包，并核对 SHA-256 摘要。
