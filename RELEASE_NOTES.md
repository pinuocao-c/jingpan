净盘 0.3.0 是一次功能、界面与安全体验更新：在继续坚持“默认进回收站、只清理可确认文件”的边界下，加入 C 盘文件迁移、图片/视频缩略预览、微信与 QQ 聊天文件清理增强，并重做透明玻璃视觉与动效。

## 本次更新

- 新增“从 C 盘迁移到 D 盘”：只推荐个人目录中更适合迁移的文件，跳过 AppData、聊天数据、云盘同步目录、链接目录、配置、数据库和系统关键路径
- 迁移采用复制、校验、再删除的事务流程；如果删除 C 盘源文件失败，会自动移除 D 盘副本，只保留 C 盘原件，避免出现两边残留或误判释放空间
- 图片、视频、Word、PPT、PDF、TXT、表格、压缩包和安装包等个人文件分类更完整，文件列表整行可点击打开预览或定位
- 图片和视频列表加入缩略图；视频会优先读取封面或首帧，便于用户在删除前确认内容
- 微信与 QQ 聊天文件清理继续增强，按账号、来源和文件类型归类，重点覆盖聊天图片、视频、文档、PPT、PDF、压缩包和安装包
- 重复文件治理保留 SHA-256 校验，不自动删除，用户确认后才移入回收站
- 将软件底色和接近白色的单一浅蓝背景拆成两个独立层级；首页主面板、指标页签、推荐操作、设置面板和按钮均不铺设白色或蓝色填充，背景直接透过，并用 5–7px 的背景采样、上亮下暗的厚边、局部反光和悬浮投影表现立体清玻璃
- 删除大圆弧和多层蓝色光斑，只保留极浅的单色明暗过渡；滚动时透明玻璃会自然透出固定背景的不同区域
- 修正鼠标局部高光坐标：改为相对于实际页面背景计算，侧栏宽度、顶部栏高度和窗口缩放不再造成光斑偏移
- 重绘应用图标，保留原有扫帚与闪光标识，加入双层玻璃边框、顶部反光、底部厚度与立体阴影
- 首页磁盘使用率圆环升级为白色透明玻璃仪表盘，加入真正透明的中心镜片、独立环形进度层、内外双边、上沿反光与立体厚度；已用与剩余空间使用不同透明度区分
- 新增完整深色模式，应用内容与 Windows 原生标题栏会自动跟随系统主题
- 首页磁盘指标改为非等宽布局，推荐操作改为更有节奏的响应式 Bento 结构
- 新增页面进入、局部高光、导航切换、按钮按压、弹层出现、进度高光和磁盘圆环进入动效
- 设置页全部选项支持整行透明高光、图标抬升与旋转、右侧状态或操作键跟随反馈；整块透明面板不做位移动画，避免 Electron 透明滤镜合成时偶发文字闪烁
- 动效不持续修改布局；透明玻璃主体保持稳定，变换只用于不承担文字合成的图标与高光层
- 最小窗口下推荐操作自动收为单列，磁盘百分比圆环在滚动与不同缩放下保持固定结构
- 统一按钮、卡片、输入框、标签和弹层的圆角、阴影、边框与系统蓝主色
- 保留清晰的键盘焦点、危险操作颜色、可恢复提示和文件表格信息密度

## 可访问性与性能

- 尊重 Windows“减少动态效果”设置，关闭页面进入、弹层、进度高光等非必要动画
- 为减少透明度模式提供不透明实色回退，关闭背景模糊后文字仍保持清晰
- 支持 Windows 高对比度强制颜色模式，关键面板恢复为系统边框和系统背景
- 深色和浅色界面均使用同一系统蓝操作层级，不在页面中间切换视觉主题
- 没有新增运行时 UI 依赖；新增扫描与迁移能力均走现有安全白名单、路径规范化和二次校验机制

## 关于 Liquid Glass

Apple 的原生 Liquid Glass 材质只提供给 Apple 平台。净盘是 Electron Windows 应用，本次实现使用 WebGL 背景采样、透明材质、边缘折射、高光、厚度阴影和系统主题完成相近的视觉效果，并非 Apple 官方组件。

## 安装说明

正式发布后，旧版净盘会显示 `v0.3.0` 更新提醒。点击“下载更新”后，净盘会使用默认浏览器打开 GitHub 官方安装包下载，下载完成后请手动运行安装程序。

当前安装包尚未进行商业代码签名，因此 Windows SmartScreen 可能显示“未知发布者”。请只使用本项目生成或 GitHub Release 页面提供的安装包，并核对 SHA-256 摘要。
